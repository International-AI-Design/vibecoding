# <Project Name> — CLAUDE.md

> **Template.** Copy to the root of a project, fill in, **check it into git.**
>
> This file is loaded automatically whenever Claude Code runs in this repo. It's the project's durable working memory — not documentation for humans. Optimize it for a model that knows nothing about this codebase and has to be productive in 90 seconds.

---

## WHY (read first)

<!-- The mission this code serves. Not what it does — what it's FOR.
     If a session can't tell what outcome the work serves, it optimizes for the wrong thing convincingly. -->

**What this is:**
**Who it's for:**
**What "good" looks like:**

---

## CURRENT STATE

> **The load-bearing section.** If anything below contradicts a general statement later in this file, **this section wins.**
>
> Update it whenever a load-bearing fact changes. Bloat is acceptable here; **omission is not.**

**Last updated:** <YYYY-MM-DD>

- **What's live:** <what's deployed, where, and how you'd verify that claim in 10 seconds>
- **What's in flight:** <current work, branch names, open PRs>
- **What's broken / known-bad:** <the things that will waste a session if undiscovered>
- **What's next:** <the actual next task>

<!-- Date every claim. "Recently" is a time bomb — it was true when written and is a lie forever after. -->

---

## Stack

- **Language / runtime:**
- **Framework:**
- **Data:**
- **Deploy:**
- **Tests:** `<the exact command>`
- **Typecheck:** `<the exact command>`
- **Run locally:** `<the exact command>`

<!-- Exact commands. Not "run the tests" — the literal string, so nobody guesses. -->

---

## How to verify your work here

<!-- THE most valuable section for output quality. Layer 2 of the method.
     Name the real mechanism for each kind of change. Be specific. -->

| Change type | How to verify |
|---|---|
| UI | |
| API / handler | |
| Schema / data | |
| Bug fix | |

**Definition of Done:** every acceptance criterion verified **by execution**. "Tests pass" is necessary, not sufficient.

---

## Discipline rules

<!-- The things that are true here and would NOT be guessed correctly.
     Each rule should have cost someone real time — otherwise it's noise. -->

1.
2.
3.

---

## Hot files — coordinate before editing

<!-- Files where two people editing at once causes real pain. -->

- `path/to/file` — <why it's hot>

---

## Corrections

<!-- Every time Claude gets something wrong in THIS repo, add a line.
     This is the highest-signal content in the file. Per Boris Cherny: edit it
     several times a week; that's the loop that makes the environment learn. -->

-

---

## Gotchas

<!-- Counterintuitive things. The stuff where the obvious move is the wrong one. -->

-
